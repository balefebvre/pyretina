from setuptools import setup



packages = ['pyretina']

setup(
    name='pyretina',
    version='0.1.0',
    packages=packages,
    install_requires=[
        'matplotlib',
        'numpy',
    ]
)
